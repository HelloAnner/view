{
  "name": "view",
  "version": "0.1.0",
  "description": "A read-only browser workspace for previewing local files and directories.",
  "module": "index.ts",
  "type": "module",
  "bin": {
    "v": "./dist/cli.js"
  },
  "scripts": {
    "build": "bun build src/cli.ts --outdir dist --target node",
    "compile": "bun build src/cli.ts --outfile dist/view --compile",
    "dev": "bun run src/cli.ts",
    "test": "bun test"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "keywords": [
    "markdown",
    "preview",
    "cli",
    "viewer"
  ],
  "license": "MIT",
  "dependencies": {
    "@fontsource-variable/jetbrains-mono": "^5.2.8",
    "commander": "^12.1.0",
    "highlight.js": "^11.11.1",
    "marked": "^15.0.11",
    "marked-highlight": "^2.2.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
