# Project working preferences

- After completing any development change in this repository, run `make install` before handing the work back.
- Installation must not use `sudo`; the default install prefix is `$(HOME)/.local`.
- Verify that a fresh login shell resolves `v` to `$(HOME)/.local/bin/v` and that the installed binary starts successfully.
